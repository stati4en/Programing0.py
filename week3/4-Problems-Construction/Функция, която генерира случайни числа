#random_numbers.py
def generate_random_list(n,start,end):
     from random import randint

     list_ns = []
     index = 0
     while index < n :
          num = randint(start , end)
          num = int(num)
          list_ns += [num]
          index += 1

     return list_ns
